@model IEnumerable<Commercial.tblSupplier>

@{
    ViewBag.Title = "Supplier";
    Layout = "~/Views/Shared/_Layout.cshtml";
}



<div class="row" style="margin-top: 25px; margin-bottom: 25px;">
    <div class="col-md-12">
        <h3>Supplier List</h3>
        <div class="float-right" style="margin-bottom:5px">
            @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-primary btn-sm" })
        </div>
        <table class="table table-striped table-bordered" id="suppliertable">
            <thead style="text-align: center">
                <tr>
                    <th>Name</th>
                    <th>Address</th>
                    <th>Contact No</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody id="tableBody" style="text-align: center">
                @foreach (tblSupplier item in Model)
                {
                    <tr>
                        <td>@item.Name</td>
                        <td>@item.Address</td>
                        <td>@item.ContactNo</td>
                        <td>
                            <a class="btn btn-info btnde2" href="@Url.Action("Edit","Suppliers", new { id = item.Id })"><i class="fas fa-edit"></i></a>
                            <a class="btn btn-danger" href="@Url.Action("Delete","Suppliers", new { id = item.Id })"><i class="fas fa-trash-alt"></i></a>

                        </td>
                    </tr>
                }
            </tbody>

        </table>
    </div>
</div>
@section scripts{
    <script type="text/javascript">
        $(document).ready(function () {
            var table = $('#suppliertable').DataTable();
        });

        $('#suppliertable').DataTable({
            responsive: true
        });


    </script>
}
