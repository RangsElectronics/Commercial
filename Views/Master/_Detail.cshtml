@model Commercial.Models.MasterViewModel

@if (Model != null && Model.DetailList != null)
{
    for (var x = 0; x < Model.DetailList.Count(); x++)
    {
        @Html.Partial("_PartialDetail", Model.DetailList[x]);
    }

}
